import recreateTransportationGraph from '../logic/recreateTransportationGraph'

export default describe('test', () => {
    it('it works', async () => {
        // const graph = recreateTransportationGraph();
        return true;
      });
})
  